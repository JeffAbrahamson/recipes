% Needed to produce clean date on title page
\usepackage[UKenglish]{isodate}

%% Supported screen resolutions
% ipad 2 -- 1024 × 768
% ipad   -- 2048 by 1536
% galaxy tab 10.1 --1280 × 800
% galaxy GS2 -- 720 x 1280
% galaxy GS3 -- 1920 × 1080
% iphone 4 -- 640 × 960
% iphone 5 -- 640 × 1136

% for A4
%\usepackage[papersize={148mm,210mm},top=35mm,bottom=8mm,twoside,bindingoffset=20mm,left=12.7mm,right=30mm]{geometry}
%\usepackage[papersize={720pt,1280pt},paperwidth=720pt,paperheight=1280pt,top=5pt,bottom=5pt,left=3pt,right=3pt]{geometry}
% 720 x 1280 --> 360 x 640
%\usepackage[papersize={360pt,640pt},paperwidth=72pt,paperheight=128pt,top=5pt,bottom=5pt,left=3pt,right=3pt]{geometry}
\usepackage[papersize={360pt,640pt},top=5pt,bottom=5pt,left=3pt,right=3pt]{geometry}

%% Debian's latex installation (tetex) used to use fancyheaders
%\usepackage{fancyheadings}
%% But with the transition to TeX Live, it wants fancyhdr
\usepackage{fancyhdr}

%% If these lengths are set here (instead of in \usepackage{geometry},
%% above, the page doesn't center properly.
% \setlength{\paperheight}{8.5in}
% \setlength{\paperwidth}{5.5in}
% \setlength{\textwidth}{4.25in}
% \setlength{\textheight}{7in}
% \setlength{\oddsidemargin}{1.25in}
% \setlength{\evensidemargin}{.5in}

\thispagestyle{empty}
\setlength{\parskip}{8pt}
\raggedright

\pagestyle{fancyplain}
%\addtolength{\headwidth}{\marginparsep}
%\addtolength{\headwidth}{\marginparwidth}
%   Remember chapter title
\renewcommand{\chaptermark}[1] {\markboth{#1}{#1}}
%   section number and title
%\renewcommand{\sectionmark}[1] {\markright{\thesection\ #1}}
\renewcommand{\sectionmark}[1] {\markright{\thechapter\ #1}}
\lhead[\fancyplain{}{\bfseries\thepage}]%
      {\fancyplain{}{\bfseries\rightmark}}
\rhead[\fancyplain{}{\bfseries\leftmark}]%
      {\fancyplain{}{\bfseries\thepage}}
\cfoot{}
